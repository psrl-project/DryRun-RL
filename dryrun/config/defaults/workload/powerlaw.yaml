# Output-length distribution name.
name: powerlaw

# Number of request lengths to sample.
n: 200

# RNG seed.
seed: 42

# Power-law exponent (larger = more short requests).
alpha: 2.0

# Inclusive lower bound of generated tokens.
lo: 10

# Inclusive upper bound of generated tokens.
hi: 1000
