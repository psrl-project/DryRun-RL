# Output-length distribution.
output:
  name: uniform

  # Number of request lengths to sample.
  n: 200

  # RNG seed.
  seed: 42

  # Inclusive lower bound of generated tokens.
  lo: 50

  # Inclusive upper bound of generated tokens.
  hi: 200

# Prompt-length distribution. Defaults to a fixed length for every request.
prompt:
  name: fixed

  # Number of request lengths to sample.
  n: 200

  # RNG seed.
  seed: 42

  # Fixed prompt length in tokens.
  lo: 512
  hi: 512
